---
description: Run wild: dispatch autonomous task into a disposable Bubblewrap Git sandbox using bws
---
Please use the `bws` skill to execute the following task with an autonomous agent running in an isolated Git sandbox:

```bash
bws gw --stash -- agy-run-wild --effort high -p "/goal $ARGUMENTS"
```

Task to execute:
$ARGUMENTS
